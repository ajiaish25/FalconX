# ============================================
# Session Secret Key (for password encryption)
# ============================================
SESSION_SECRET_KEY=tao123cdk789

# ============================================
# LDAP Authentication Configuration
# ============================================
# Service account for searching users in LDAP
# Password is encrypted - run generate_env.py to encrypt
LDAP_SERVICE_ACCOUNT_USERNAME=global\SVC_FalconX
LDAP_SERVICE_ACCOUNT_PASSWORD=6FKGZVL7Jmp8QTRW2r8s  # Encrypted password here
LDAP_SERVER_URL=ldaps://ORDPGBLADC01.global.cdk.com:636

# ============================================
# Jira Service Account (Common Credentials)
# ============================================
# These credentials are used for ALL users after LDAP login
# Password is encrypted - run generate_env.py to encrypt
JIRA_SERVICE_EMAIL=ajith.ramesh@cdk.com
JIRA_SERVICE_API_TOKEN=******  # Encrypted password here
JIRA_BASE_URL=https://projects.cdk.com
```````````````````````````````
# ============================================
# Confluence Service Account (Common Credentials)
# ============================================
# These credentials are used for ALL users after LDAP login
# Password is encrypted - run generate_env.py to encrypt
CONFLUENCE_SERVICE_EMAIL=ajith.ramesh@cdk.com
CONFLUENCE_SERVICE_API_TOKEN=******  # Encrypted password here
CONFLUENCE_BASE_URL=https://projects.cdk.com/wiki

# ============================================
# OpenAI / Databricks AI Configuration
# ============================================
# Databricks OpenAI-compatible endpoint configuration
# IMPORTANT: Do NOT commit real workspace keys or tokens here.
# Use placeholders in this template and put real secrets only in backend/config/.env (which is gitignored).
OPENAI_API_KEY=your-databricks-openai-api-key-here
OPENAI_API_ENDPOINT=https://your-databricks-workspace-url/serving-endpoints/databricks-gpt-5-1/invocations
OPENAI_MODEL=databricks-gpt-5-1

# ============================================
# Setup Instructions:
# ============================================
# 1. Copy this file to .env (remove .template)
# 2. Run: python backend/scripts/generate_env.py
# 3. Copy the encrypted passwords to this file (replace ****** placeholders)
# 4. Update LDAP_SERVICE_ACCOUNT_USERNAME with actual username (replace svc_*****)
# 5. Change SESSION_SECRET_KEY in production (currently: tao123cdk789)


